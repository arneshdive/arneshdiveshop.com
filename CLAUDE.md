# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

> The import above pulls in Next.js-version-specific agent rules (this project pins Next.js 16, which has breaking changes vs. training data — consult `node_modules/next/dist/docs/` before writing App Router code).

## Project State

This is **Arne's Dive Shop**, a freediving/scuba e-commerce platform, currently in early bootstrap: the `app/` directory is still the unmodified `create-next-app` scaffold. The real architecture and requirements live in the planning docs under `docs/sandwich/` and the UI reference lives in `docs/wireframes/`. Read those before writing feature code — they are the spec, not just background reading.

## Commands

Package manager is **pnpm** (`pnpm-workspace.yaml` + `pnpm-lock.yaml` present — don't use npm/yarn).

```bash
pnpm dev      # start dev server (localhost:3000)
pnpm build    # production build
pnpm start    # run production build
pnpm lint     # eslint (flat config: eslint.config.mjs)
```

No test runner is configured yet.

## Planning docs (`docs/sandwich/`)

Generated by the `sandwich` pipeline (order → prep → build). Treat these as living specs:

- `prd.md` — modules/features list with confidence tags (`[stated]` = locked requirement, `[inferred]` = needs confirmation, `[assumed]` = must be validated before building). Check a feature's tag before assuming scope.
- `technical-notes.md` — stack choices and architecture rationale (see below), plus a **Risks** and **Open Decisions** section.
- `feature-queue.md` — prioritized backlog (F-001..F-020) with status; check this before picking up new work.
- `client-questions.md` — unresolved questions blocking specific modules (currently: domestic/overseas shipping calculation method, analytics platform choice — both unresolved, don't hardcode assumptions around them).
- `user-flows.md` — step-by-step actor flows (UF-xxx) for customer/admin journeys.
- `specs/` — per-feature spec files referenced from the feature queue.

## Wireframes (`docs/wireframes/`)

Static Tailwind CSS HTML mockups, desktop-first, **all UI copy in Bahasa Indonesia** — match that when implementing corresponding pages/components. Cover the full storefront (`homepage`, `plp*`, `pdp`, `cart`, `checkout`, `search`, `account*`, `wishlist`, `contact`, `faq`, `login`/`register`/`otp`, legal pages) and the full admin panel (`admin/`: dashboard, products, categories, brands, orders, customers, promotions, banners, inventory, reports, settings, users). These define page layout/structure — use them as the UI source of truth rather than inventing new layouts.

## Target Architecture (per `docs/sandwich/technical-notes.md`)

**Stack:** Next.js (App Router) + TypeScript strict, Tailwind CSS v4, Drizzle ORM + PostgreSQL, Zod, NextAuth.js v5 (email/password + Google OAuth), Midtrans (Indonesian payment gateway), React Hook Form + Zod resolver, Radix UI primitives, TanStack Query v5 (admin only), Mailgun (transactional email/OTP), Vercel Blob (product images), deployed on Vercel.

**Vertical slice + clean architecture:** each feature lives as a self-contained module under `features/`. Complex features (products, orders, payment) get full domain/application/presentation layering; simple features (banners, categories) stay presentation-only with direct DB queries — don't add layers speculatively. Domain layer is pure TypeScript with zero dependencies; application layer uses direct Drizzle queries (no repository pattern for plain CRUD); no infrastructure abstraction layer beyond the payment provider interface.

**Storefront vs Admin split** — this is the central architectural decision, don't blur it:
- **Storefront**: ISR + Server Components for SEO. Queries are direct function imports inside Server Components (no HTTP round-trip). Mutations go through Server Actions.
- **Admin**: CSR with TanStack Query. Admin data is served via Route Handlers (HTTP) for caching/parallelization, not direct Server Component queries.

**Money as integer cents**: all prices stored as `priceCents` (integer) in the DB to avoid float errors, converted to dollars/rupiah at API boundaries, formatted for display via a `formatCurrency` helper. Never store or compute prices as floats.

**Orders**: created atomically — order + orderItems + payment record in a single DB transaction, with an idempotency key to prevent duplicate orders from double form-submits. Status workflow: `pending_payment → processing → shipped → delivered`.

**Payment**: `PaymentProvider` interface abstracts Midtrans so it's swappable (e.g. for Stripe/Xendit) without touching order logic. Webhook handler must verify the signature before updating order status.

**No inventory tracking** — explicitly dropped by the client (flagged as a risk in technical-notes.md if requirements change later); products use a single `isActive` availability flag instead of stock counts.

## Other directories

- `.sandwich/` — sandwich pipeline registry/state (features, decisions, questions); don't hand-edit, it's projected into the `docs/sandwich/*.md` files.
- `docs/superpowers/specs/` and `docs/superpowers/plans/` — planning artifacts from prior sessions (e.g. wireframe design spec).
