# F-002: Database schema and migrations

> **Module:** Infrastructure · **Priority:** 20/100 · **Depends on:** F-001 · **Source:** prd.md:database-schema

Define all tables using Drizzle ORM with pgEnum types for orders, payments, promotions, banners

## Scope

**In:**
- Users table with role enum
- Categories table with parent/child hierarchy
- Brands table
- Products and product_variants tables
- Customers and addresses tables
- Orders and order_items tables
- Payments table with status enum
- Promotions table with type enum
- Banners table with position enum
- Sessions and accounts tables for Auth.js
- pgEnum definitions for all status/type fields
- Drizzle migrations setup

**Out:**
- Seed data
- Query functions

## Acceptance Criteria

- [ ] **AC1** — All tables from SPEC.md schema created in database
- [ ] **AC2** — All pgEnum types defined and applied
- [ ] **AC3** — Foreign key relationships established correctly
- [ ] **AC4** — Drizzle migrations can be run with drizzle-kit push

---

## Mulai kerja

1. Jalankan `superpowers:brainstorming` dengan file ini sebagai starting point —
   scope & AC di atas adalah requirement; task breakdown dan pilihan teknis
   diputuskan di sesi brainstorming (dengan akses codebase).
2. Setelah implementasi, centang tiap AC yang sudah terbukti jalan
   (update `done` di `F-002.json`, lalu jalankan ulang render-specs).

_Generated by /prep — edit `F-002.json`, bukan file ini (akan di-overwrite)._
