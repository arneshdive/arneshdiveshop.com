# F-001: Database schema & connection setup

> **Module:** Infrastructure · **Priority:** 26/100 · **Depends on:** — · **Source:** technical-notes.md:28-29

Drizzle ORM schema with all 13 tables connected to Neon Postgres. Schema implemented, migrations generated, all tables active in production database.

## Scope

**In:**
- Drizzle schema definitions for all tables listed in technical-notes.md (users, customers, addresses, categories, brands, products, product_variants, orders, order_items, payments, promotions, banners, verification_tokens)
- Neon Postgres connection setup (lib/db)
- Initial Drizzle migration generation and run against Neon
- priceCents stored as integer on all money-bearing columns

**Out:**
- Seed data / fixtures
- Read replicas or connection pooling beyond default Neon serverless driver behavior

## Acceptance Criteria

- [x] **AC1** — lib/db exists with a working Drizzle client connected to Neon
- [x] **AC2** — Schema covers every table named in technical-notes.md architecture notes
- [x] **AC3** — Drizzle migrations run cleanly against a fresh Neon database with `drizzle-kit push` or `migrate`
- [x] **AC4** — All price/money columns are integer (cents), never float

## Implementation Notes

- Schema was implemented prior to this documentation update
- Migration file generated: `lib/db/migrations/0000_married_microbe.sql`
- All 13 tables verified present in Neon database
- TypeScript types inferred via Drizzle's `$inferSelect` and `$inferInsert`
