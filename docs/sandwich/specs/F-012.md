# F-012: Order creation with atomic transaction

> **Module:** Orders · **Priority:** 11/100 · **Depends on:** F-003, F-008, F-010, F-011 · **Source:** prd.md:orders-module

Order creation with idempotency keys, atomic order+items+payment insert, status workflow

## Scope

**In:**
- Order creation Server Action
- Atomic transaction: order + items + payment record
- Idempotency key generation and validation
- Order number generation
- Order status enum and workflow
- Order confirmation email via Mailgun
- Order history for customers
- Admin order list with filters

**Out:**
- Order refunds
- Order cancellation by customer

## Acceptance Criteria

- [ ] **AC1** — Order created with all items in single atomic transaction
- [ ] **AC2** — Duplicate submission prevented by idempotency key
- [ ] **AC3** — Order number auto-generated with format ORD-XXXXX
- [ ] **AC4** — Customer receives order confirmation email
- [ ] **AC5** — Customer can view order history in account
- [ ] **AC6** — Admin can view and filter orders

---

## Mulai kerja

1. Jalankan `superpowers:brainstorming` dengan file ini sebagai starting point —
   scope & AC di atas adalah requirement; task breakdown dan pilihan teknis
   diputuskan di sesi brainstorming (dengan akses codebase).
2. Setelah implementasi, centang tiap AC yang sudah terbukti jalan
   (update `done` di `F-012.json`, lalu jalankan ulang render-specs).

_Generated by /prep — edit `F-012.json`, bukan file ini (akan di-overwrite)._
